apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-johnpofo-monapp3-frontend-ingress
  namespace: staging-johnpofo-monapp3-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.mydomain.flapi.org
      secretName: staging-johnpofo-monapp3-frontend-tls
  rules:
    - host: staging.mydomain.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-johnpofo-monapp3-frontend-service
                port:
                  number: 3000
