apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-johnpofo-monapp3-frontend-ingress
  namespace: prod-johnpofo-monapp3-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - mydomain.flapi.org
      secretName: prod-johnpofo-monapp3-frontend-tls
  rules:
    - host: mydomain.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: prod-johnpofo-monapp3-frontend-service
                port:
                  number: 3000
