apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-johnpofo-monapp3-frontend-deployment
  namespace: prod-johnpofo-monapp3-frontend
  labels:
    app: prod-johnpofo-monapp3-frontend
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: prod-johnpofo-monapp3-frontend
  template:
    metadata:
      labels:
        app: prod-johnpofo-monapp3-frontend
    spec:
      containers:
        - name: prod-johnpofo-monapp3-frontend
          image: corentin35/johnpofo-monapp3-frontend:${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: '1'
              memory: '1Gi'
            limits:
              cpu: '2'
              memory: '2Gi'
      imagePullSecrets:
        - name: regcred
