apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-johnpofo-monapp3-frontend-ingress
  namespace: dev-johnpofo-monapp3-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - dev.mydomain.flapi.org
      secretName: dev-johnpofo-monapp3-frontend-tls
  rules:
    - host: dev.mydomain.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: dev-johnpofo-monapp3-frontend-service
                port:
                  number: 3000
